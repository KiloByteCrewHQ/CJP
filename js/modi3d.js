/* No longer used — kept as an empty stub so any stale <script> reference can't 404.
   The Modi hero is now rendered as a PNG with an SVG spray overlay in index.html /
   css/style.css. Safe to delete this file. */
